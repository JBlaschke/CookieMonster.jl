var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Public","page":"API reference","text":"The package exports a single entry point:","title":"Public"},{"category":"section","location":"api/#Internal","page":"API reference","text":"These functions are not exported and are documented for reference; they are not part of the public API and may change between releases.","title":"Internal"},{"category":"section","location":"api/#Index","page":"API reference","text":"","title":"Index"},{"category":"module","location":"api/#CookieMonster.CookieMonster","page":"API reference","text":"CookieMonster\n\nRead, decrypt, and write cookies for Chromium-based browsers (Chrome, Chromium, Brave) on macOS and Linux.\n\nThe browser stores its cookies in an SQLite database, with each cookie value encrypted using AES-128-CBC. The encryption key is derived (via PBKDF2-HMAC-SHA1) from a per-browser \"Safe Storage\" password held in the OS keyring — the macOS Keychain, or a Linux Secret Service keyring — with a well-known fallback on Linux.\n\nThe exported entry points are read_cookies and write_cookie; the latter encrypts a value the same way the browser does and inserts it into the store.\n\n\n\n\n\n","title":"CookieMonster.CookieMonster"},{"category":"function","location":"api/#CookieMonster.read_cookies","page":"API reference","text":"read_cookies(browser = \"chrome\"; profile = \"Default\", domain = nothing,\n             base = nothing, keys = nothing) -> Vector{NamedTuple}\n\nRead and decrypt the cookies stored by browser (\"chrome\", \"chromium\", or \"brave\", case-insensitive).\n\nEach returned element is a named tuple with fields host, name, path, value (decrypted), expires (a DateTime or nothing), secure, and httponly.\n\nKeyword arguments\n\nprofile: the browser profile to read (default \"Default\").\ndomain: if given, only cookies whose host contains this substring are returned.\nbase: override for the browser's data directory (see cookie_db_path).\nkeys: precomputed decryption keys (see derive_keys); derived automatically when omitted, which may prompt for keyring access.\n\nThe live database is snapshotted before reading (see snapshot), so the browser need not be closed.\n\n\n\n\n\n","title":"CookieMonster.read_cookies"},{"category":"function","location":"api/#CookieMonster.write_cookie","page":"API reference","text":"write_cookie(browser = \"chrome\"; host, name, value, kwargs...) -> Nothing\nwrite_cookie(browser, cookie::NamedTuple; kwargs...) -> Nothing\n\nEncrypt value and write a cookie into browser's on-disk cookie store — the inverse of read_cookies. The second form takes a NamedTuple shaped like an element returned by read_cookies (fields host, name, value, path, expires, secure, httponly), so a cookie can be read, modified, and written back; any keyword argument overrides the matching tuple field.\n\nAny existing cookie with the same (host, name, path) is replaced, so repeated writes are idempotent and never leave duplicates.\n\nRequired keyword arguments\n\nhost: the cookie host / domain, stored in host_key (e.g. \".github.com\").\nname: the cookie name.\nvalue: the plaintext value (encrypted before storage).\n\nOptional keyword arguments\n\npath (\"/\"): the path scope.\nexpires (nothing): a DateTime (UTC) expiry, or nothing for a session cookie. Browsers usually discard persisted session cookies on restart, so pass an expiry for a cookie that should survive one.\nsecure (false), httponly (false): the corresponding attributes. When secure is set, the row's source_scheme/source_port are written as https/443 rather than http/80.\nsamesite (:lax): one of :unspecified, :none, :lax, :strict.\nprofile (\"Default\"), base (nothing): select the profile / data directory, exactly as in read_cookies and cookie_db_path.\nkeys (nothing): precomputed keys from derive_keys; derived automatically when omitted, which may prompt for keyring access.\nscheme (nothing): :v10 or :v11; when omitted, :v11 is used if that key is available (a Linux keyring) and :v10 otherwise (always on macOS).\nhost_prefix (true): prepend the host digest to the value (Chromium v104+); see encrypt_value.\nallow_running (false): by default the write is refused when browser looks like it is running (see browser_running), because the browser's in-memory cookie store would overwrite a direct database write. Set true to override the check.\nbackup (true): copy the database to <Cookies>.cmbak before writing.\n\nwarning: Quit the browser first\nWriting to the live database while the browser is open is unreliable and can lose the write or disturb the store. The allow_running guard is a convenience, not a guarantee.\n\n\n\n\n\n","title":"CookieMonster.write_cookie"},{"category":"method","location":"api/#CookieMonster.write_cookies-Tuple{AbstractString, Any}","page":"API reference","text":"write_cookies(browser, cookies; kwargs...) -> Int\n\nEncrypt and write many cookies into browser's store in a single transaction, returning the number written. cookies is any iterable of items exposing at least host, name, and value (for example the NamedTuples returned by read_cookies); the optional fields path, expires, secure, httponly, and samesite default when absent. This is the batch form of write_cookie: the running-browser check and the <Cookies>.cmbak backup happen once, and either every cookie is written or none is.\n\nEach cookie replaces any existing one with the same (host, name, path).\n\nKeyword arguments\n\nprofile (\"Default\"), base (nothing): profile / data directory, as in read_cookies and cookie_db_path.\nkeys (nothing): precomputed keys (see derive_keys); derived automatically when omitted, which may prompt for keyring access.\nscheme (nothing): :v10 or :v11; auto-selected when omitted.\nhost_prefix (true): prepend the host digest (Chromium v104+); see encrypt_value.\nsamesite (:lax): default SameSite for items that do not specify one.\nallow_running (false): write even if the browser looks like it is running (see browser_running).\nbackup (true): copy the database to <Cookies>.cmbak first.\n\nwarning: Quit the browser first\nThe same live-database caveat as write_cookie applies.\n\n\n\n\n\n","title":"CookieMonster.write_cookies"},{"category":"method","location":"api/#CookieMonster._cookie_fields-Tuple{Any, Any}","page":"API reference","text":"_cookie_fields(c, samesite_default) -> NamedTuple\n\nNormalize a cookie-like item to the full field set write_cookies needs: host, name, and value are required, while path, expires, secure, httponly, and samesite default when the item does not provide them (so a bare (host, name, value) works, as does a full read_cookies tuple).\n\n\n\n\n\n","title":"CookieMonster._cookie_fields"},{"category":"method","location":"api/#CookieMonster.browser_running-Tuple{AbstractString}","page":"API reference","text":"browser_running(browser; base = nothing) -> Bool\n\nHeuristically report whether browser currently holds the profile open, by testing for the SingletonLock marker Chromium keeps in its user-data directory while running. A true result is reliable; a false result is not a guarantee (the marker can linger after a crash, or be briefly absent), so it is used only to steer write_cookie away from the common footgun of writing under a live browser.\n\n\n\n\n\n","title":"CookieMonster.browser_running"},{"category":"method","location":"api/#CookieMonster.build_row-Tuple{Any}","page":"API reference","text":"build_row(db; provided...) -> Vector{Pair{String, Any}}\n\nAssemble the column/value pairs for an INSERT into the cookies table of db, resilient to the schema differences between Chromium versions.\n\nThe table is introspected with PRAGMA table_info. For each column: if a value was provided, it is used; otherwise, if the column is NOT NULL with no default, a zero_for value is supplied so the insert cannot fail on a column we did not anticipate. Nullable or defaulted columns we do not set are omitted and left to SQLite. provided values whose column does not exist in this schema are dropped, so the same call works across versions.\n\n\n\n\n\n","title":"CookieMonster.build_row"},{"category":"method","location":"api/#CookieMonster.chrome_time-Tuple{Any}","page":"API reference","text":"chrome_time(x) -> Union{DateTime, Nothing}\n\nConvert a Chromium timestamp x (microseconds since 1601-01-01 UTC) to a DateTime, returning nothing for a missing or zero value (e.g. a session cookie with no expiry).\n\n\n\n\n\n","title":"CookieMonster.chrome_time"},{"category":"method","location":"api/#CookieMonster.cookie_db_path-Tuple{AbstractString}","page":"API reference","text":"cookie_db_path(browser; profile = \"Default\", base = nothing) -> String\n\nReturn the path to the Cookies SQLite database for browser and profile.\n\nbase overrides the default per-browser data directory. Recent Chromium versions store the database at <profile>/Network/Cookies, older ones at <profile>/Cookies; both locations are checked, in that order. Throws an error if neither exists.\n\n\n\n\n\n","title":"CookieMonster.cookie_db_path"},{"category":"method","location":"api/#CookieMonster.datetime2chrome-Tuple{Nothing}","page":"API reference","text":"datetime2chrome(dt) -> Int\n\nConvert a DateTime (UTC) to a Chromium timestamp (microseconds since 1601-01-01 UTC). nothing maps to 0, matching how chrome_time reads a zero timestamp back as nothing (a session cookie with no expiry). Inverse of chrome_time.\n\n\n\n\n\n","title":"CookieMonster.datetime2chrome"},{"category":"method","location":"api/#CookieMonster.db_path-Tuple{AbstractString}","page":"API reference","text":"db_path(browser) -> String\n\nReturn the default per-browser cookie data directory for browser.\n\nhomedir() is evaluated here, at call time, on purpose: this package ships as a PackageCompiler app, and the module body runs at build time. Interpolating homedir() into a top-level const would bake the build machine's home directory (e.g. /Users/runner) into the shipped image. Deferring the call to runtime resolves the invoking user's home instead.\n\n\n\n\n\n","title":"CookieMonster.db_path"},{"category":"method","location":"api/#CookieMonster.decrypt_value-NTuple{4, Any}","page":"API reference","text":"decrypt_value(enc, plainval, host_key, keys) -> String\n\nDecrypt a single cookie's stored value.\n\nenc is the raw encrypted_value blob and plainval the legacy plaintext value column; host_key is the cookie's host, and keys the named tuple of derivation keys from derive_keys.\n\nWhen enc is empty the plaintext plainval is returned. Otherwise the leading version tag (v10 or v11) selects the key, and the remaining bytes are decrypted with AES-128-CBC and PKCS#7-unpadded. Chromium (v104+) prefixes the plaintext with the SHA-256 of the host key; that prefix is stripped when present. Returns an empty string if the required key is unavailable or the ciphertext is malformed.\n\n\n\n\n\n","title":"CookieMonster.decrypt_value"},{"category":"method","location":"api/#CookieMonster.derive_keys-Tuple{AbstractString}","page":"API reference","text":"derive_keys(browser) -> NamedTuple\n\nDerive the AES-128 keys used to decrypt browser's cookies, returned as a named tuple (v10 = ..., v11 = ...). Each field is a 16-byte key, or nothing when that key scheme is unavailable on the current platform.\n\nOn macOS, the browser's \"Safe Storage\" password is read from the login Keychain (prompting the user once) and stretched with 1003 PBKDF2 iterations into the v10 key; v11 is unused (nothing).\n\nOn Linux, v10 is derived from the well-known fallback password \"peanuts\" (1 iteration), and v11 is derived from the Secret Service password looked up via secret-tool, or nothing when no keyring / secret-tool is available.\n\nbrowser must be one of \"chrome\", \"chromium\", or \"brave\".\n\n\n\n\n\n","title":"CookieMonster.derive_keys"},{"category":"method","location":"api/#CookieMonster.encrypt_value-Tuple{AbstractString, AbstractString, Any}","page":"API reference","text":"encrypt_value(plaintext, host_key, keys; scheme = :v10, host_prefix = true)\n    -> Vector{UInt8}\n\nProduce an encrypted_value blob in the exact format decrypt_value reads: the 3-byte version tag (\"v10\" or \"v11\", chosen by scheme) followed by AES-128-CBC(IV = 16 spaces, key, PKCS#7([sha256(host_key)] * plaintext)).\n\nThe 32-byte host digest is prepended to the plaintext when host_prefix is true (the default), matching Chromium v104+; decrypt_value strips it back off when it matches the cookie's host. Set host_prefix = false only for browsers old enough to predate domain-bound cookie values.\n\nThrows if the key for scheme is unavailable — :v11 on macOS, or either scheme when the corresponding key could not be derived.\n\n\n\n\n\n","title":"CookieMonster.encrypt_value"},{"category":"method","location":"api/#CookieMonster.pbkdf2_hmac_sha1-Tuple{Vector{UInt8}, Vector{UInt8}, Integer, Integer}","page":"API reference","text":"pbkdf2_hmac_sha1(pw, salt, iters, dklen) -> Vector{UInt8}\n\nDerive a dklen-byte key from password pw and salt using PBKDF2 with HMAC-SHA1 as the pseudo-random function and iters iterations.\n\nOnly a single output block is supported, so dklen must be <= 20 (the SHA-1 digest length); this is sufficient for the 16-byte AES keys used here.\n\nArguments\n\npw::Vector{UInt8}: the password / master key bytes.\nsalt::Vector{UInt8}: the salt bytes.\niters::Integer: the number of PBKDF2 iterations.\ndklen::Integer: the desired key length in bytes (<= 20).\n\n\n\n\n\n","title":"CookieMonster.pbkdf2_hmac_sha1"},{"category":"function","location":"api/#CookieMonster.pkcs7pad","page":"API reference","text":"pkcs7pad(data, blocksize = 16) -> Vector{UInt8}\n\nAppend PKCS#7 padding to data so its length becomes a multiple of blocksize. When the length is already a multiple, a whole block of blocksize padding bytes is added; this is the inverse of strip_pkcs7, which removes such a block.\n\n\n\n\n\n","title":"CookieMonster.pkcs7pad"},{"category":"method","location":"api/#CookieMonster.samesite_code-Tuple{Integer}","page":"API reference","text":"samesite_code(s) -> Int\n\nMap a SameSite value to the integer Chromium stores in the samesite column: :unspecified => -1, :none => 0, :lax => 1, :strict => 2. An integer is passed through unchanged.\n\n\n\n\n\n","title":"CookieMonster.samesite_code"},{"category":"method","location":"api/#CookieMonster.sha256_bytes-Tuple{Any}","page":"API reference","text":"sha256_bytes(data) -> Vector{UInt8}\n\nReturn the raw SHA-256 digest of data as bytes.\n\n\n\n\n\n","title":"CookieMonster.sha256_bytes"},{"category":"method","location":"api/#CookieMonster.snapshot-Tuple{AbstractString}","page":"API reference","text":"snapshot(dbpath) -> String\n\nCopy the cookie database at dbpath, together with its -wal and -shm sidecar files if present, into a fresh temporary directory and return the path to the copy.\n\nCopying the write-ahead log alongside the database ensures cookies that have not yet been checkpointed are still read, and avoids touching (or locking) the live database the browser is using.\n\n\n\n\n\n","title":"CookieMonster.snapshot"},{"category":"method","location":"api/#CookieMonster.strip_pkcs7-Tuple{Any}","page":"API reference","text":"strip_pkcs7(d)\n\nRemove PKCS#7 padding from the decrypted byte vector d, returning d unchanged if the trailing padding length is not a valid value in 1:16.\n\n\n\n\n\n","title":"CookieMonster.strip_pkcs7"},{"category":"method","location":"api/#CookieMonster.tobool-Tuple{Any}","page":"API reference","text":"tobool(x) -> Bool\n\nCoerce a SQLite column value to Bool, treating a missing (NULL) value as false.\n\n\n\n\n\n","title":"CookieMonster.tobool"},{"category":"method","location":"api/#CookieMonster.zero_for-Tuple{AbstractString}","page":"API reference","text":"zero_for(sqltype) -> Union{Int, String, Vector{UInt8}}\n\nA type-appropriate zero for an SQLite column declared with type sqltype: 0 for an integer affinity, an empty Vector{UInt8} for BLOB, and \"\" otherwise. Used to satisfy NOT NULL columns that write_cookie does not set itself.\n\n\n\n\n\n","title":"CookieMonster.zero_for"},{"category":"section","location":"#CookieMonster.jl","page":"Home","text":"Read, decrypt, and write cookies for Chromium-based browsers — Google Chrome, Chromium, and Brave — on macOS and Linux, directly from the browser's on-disk cookie store.\n\nEach browser keeps its cookies in an SQLite database whose values are encrypted with AES-128-CBC. The key is derived (via PBKDF2-HMAC-SHA1) from a per-browser \"Safe Storage\" password held in the operating-system keyring: the login Keychain on macOS, or a Secret Service keyring on Linux (with a well-known fallback key when no keyring is available). CookieMonster reproduces that scheme and hands you the decrypted cookies as plain Julia values.\n\nwarning: `read_cookies` decrypts your real browser secrets\nThe values returned by read_cookies include live session tokens, authentication cookies, and other credentials belonging to the browser's signed-in user. On macOS the first call prompts for Keychain access. Treat the results as sensitive data: do not log them, print them in shared terminals, or transmit them to third parties.","title":"CookieMonster.jl"},{"category":"section","location":"#Installation","page":"Home","text":"CookieMonster is not registered, so install it directly from GitHub:\n\nusing Pkg\nPkg.add(url = \"https://github.com/JBlaschke/CookieMonster.jl\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using CookieMonster\n\n# All cookies from Chrome's default profile\ncookies = read_cookies(\"chrome\")\n\n# Just the cookies for one domain\ngh = read_cookies(\"chrome\"; domain = \"github.com\")\n\nfor c in gh\n    println(c.name, \" = \", c.value, \"   (expires \", c.expires, \")\")\nend\n\nEach element is a NamedTuple with the following fields:\n\nField Type Description\nhost String Cookie host / domain (the host_key column).\nname String Cookie name.\npath String Path scope of the cookie.\nvalue String Decrypted cookie value.\nexpires DateTime or nothing Expiry, or nothing for a session cookie.\nsecure Bool Secure attribute.\nhttponly Bool HttpOnly attribute.","title":"Quick start"},{"category":"section","location":"#Writing-cookies","page":"Home","text":"write_cookie is the inverse of read_cookies: it encrypts a value exactly the way the browser does and inserts a fully-formed row into the cookie store.\n\nusing CookieMonster, Dates\n\nwrite_cookie(\"chrome\";\n    host     = \".example.com\",\n    name     = \"session\",\n    value    = \"s3cr3t-token\",\n    expires  = now(UTC) + Year(1),   # omit for a session cookie\n    secure   = true,\n    httponly = true,\n)\n\nBecause read_cookies returns NamedTuples, a cookie can be read, modified, and written back — the second form of write_cookie takes such a tuple, and keyword arguments override its fields:\n\nc = only(read_cookies(\"chrome\"; domain = \"example.com\"))\nwrite_cookie(\"chrome\", c; value = \"rotated-token\")\n\nwarning: Quit the browser before writing\nChromium keeps its cookies in an in-memory store and flushes them through to SQLite, so writing to the live database while the browser is open is unreliable — the write can be lost or clobbered. By default write_cookie refuses when it detects the browser running (see browser_running); it also backs the database up to <Cookies>.cmbak first. An existing cookie with the same (host, name, path) is replaced.\n\nThe cookies table must already exist, so the target profile must have been created by at least one prior browser run. See write_cookie for the full list of keyword arguments (path, samesite, scheme, host_prefix, profile, base, keys, …).","title":"Writing cookies"},{"category":"section","location":"#Command-line-interface","page":"Home","text":"A cookie command (built with Comonicon and PackageCompiler) wraps the library with two subcommands, read and write:\n\ncookie read chrome --domain github.com --json > cookies.json\ncookie write chrome < cookies.json          # install them (on another machine)\n\ncookie write reads the JSON array cookie read --json emits (from a file with --input, or standard input), or writes a single cookie from --host/--name/ --value. As with write_cookie, quit the browser first; the command refuses to write under a running browser unless given --allow-running. See cookie write --help for all options.","title":"Command-line interface"},{"category":"section","location":"#Supported-browsers-and-platforms","page":"Home","text":"Browser browser argument macOS Linux\nChrome \"chrome\" ✅ ✅\nChromium \"chromium\" ✅ ✅\nBrave \"brave\" ✅ ✅\n\nOn macOS the decryption password is read from the login Keychain (v10 keys). On Linux the value is decrypted with the v11 key from the Secret Service keyring when available, falling back to the v10 \"peanuts\" key otherwise.","title":"Supported browsers and platforms"},{"category":"section","location":"#Selecting-a-profile-or-database","page":"Home","text":"By default CookieMonster reads the Default profile from the browser's standard data directory. Pass profile to pick a different profile, or base to point at a data directory in a non-standard location:\n\nread_cookies(\"chrome\"; profile = \"Profile 1\")\nread_cookies(\"chromium\"; base = \"/path/to/chromium/User Data\")\n\nSee cookie_db_path for how the database file is located.","title":"Selecting a profile or database"},{"category":"section","location":"#Next-steps","page":"Home","text":"The complete list of exported and internal functions, with signatures and argument descriptions, is on the API reference page.","title":"Next steps"}]
}
